# UI work happens here

`C:\Users\saivenkat\Desktop\jarvis UI test`

Do not change this project's `app/` or `components/` until a design is finalized and copied over. See that folder's `INTEGRATE.md`.
