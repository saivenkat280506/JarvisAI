# API STT is permanently disabled as per system security policy.
# ZERO API CALLS ALLOWED FOR STT.

async def transcribe_audio_api(file_path):
    print("[API STT] Call blocked: System is in LOCAL ONLY mode.")
    return ""
